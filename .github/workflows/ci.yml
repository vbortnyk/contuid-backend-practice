name: Build and push Conduit backend

on: 
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write
      
jobs:
  call-deploy-workflow:
    uses: vbortnyk/conduit-practice/.github/workflows/deployment-submodules.yml@main
    secrets: inherit
    with:
      service: ${{ vars.BACKEND_SERVICE_NAME }}
      tag: ${{ github.sha }}
      gh-username: ${{ vars.GH_USERNAME }}  
      vps-username: ${{ vars.VPS_USERNAME }}
      vps-host: ${{ vars.VPS_HOST }}

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Login to GHCR
#         uses: docker/login-action@v4
#         with: 
#           registry: ghcr.io
#           username: ${{ vars.GH_USERNAME }}
#           password: ${{ secrets.GH_PAT }}   
      
#       - name: Build and push to GHCR
#         id: build
#         uses: docker/build-push-action@v7
#         with:
#           context: .
#           push: true
#           tags: ghcr.io/${{ vars.GH_USERNAME }}/${{ vars.BACKEND_IMAGE_NAME }}:${{ github.sha }}
        
#   deploy:
#     needs: build
#     uses: vbortnyk/conduit-practice/.github/workflows/deployment-submodule.yml@main
#     secrets: inherit
#     with:
#       service: "backend"
#       tag: ${{ github.sha }}
#       username: ${{ vars.GH_USERNAME }}

